<Window x:Class="RobotFilesEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RobotFilesEditor"
        mc:Ignorable="d"
        Title="MainWindow" Name="window" Width="775">
    <Grid DataContext="">
        <Menu x:Name="MainWindowMenu" 
              VerticalAlignment="Top" 
              FontSize="14" 
              IsTabStop="True">            
            <MenuItem Header="Files" FontSize="14">
                <MenuItem Header="Set folder paths">
                    <MenuItem Header="{Binding SelectedSourceFoldersPath, ElementName=window}" Name="FolderSourcePath" />
                    <MenuItem Header="{Binding SelectedDestFoldersPath, ElementName=window}" Name="FolderDestinationPath"/>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Controles" ItemsSource="{Binding ControlersChooserMenu.Items, ElementName=window}"/>
            <MenuItem Header="Operations" ItemsSource="{Binding OperationsMenu.Items, ElementName=window}"/>
            <MenuItem Header="Configuration"/>
        </Menu>
        <ListBox HorizontalAlignment="Left" Margin="0,24,0,0" />       
    </Grid>
</Window>
