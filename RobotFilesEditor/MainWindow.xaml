<Window x:Class="RobotFilesEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RobotFilesEditor"
        DataContext="{Binding MainVM, Source={StaticResource Locator}}"
        mc:Ignorable="d"
        Title="MainWindow" Name="window" Width="775">
    <Grid Margin="0,68,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Expander ExpandDirection="Left" IsExpanded="True" Header="Controlers" UseLayoutRounding="False" Grid.Column="0" Grid.Row="1">
            <GroupItem x:Name="ControlersChooserGroupBox"  Margin="5" Width="Auto">
                <ItemsControl Name="ControlersChooser" VerticalAlignment="Top" ItemsSource="{Binding ControlerChooser}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <RadioButton Content="{Binding Content}" GroupName="Controlers" Command="{Binding ClickedCommand}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupItem>
        </Expander>
        <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Top">
            <Expander  IsExpanded="True" Header="Move files" Visibility="{Binding MoveFilesOperationsVisibility}">
                <GroupItem x:Name="MoveFilesOperationsGroupBox"  Margin="5">
                    <ItemsControl Name="MoveFilesOperations" VerticalAlignment="Top" ItemsSource="{Binding MoveFilesOperations, NotifyOnSourceUpdated=True}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Content}" Command="{Binding ClickedCommand}" Margin="3" HorizontalContentAlignment="Left" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupItem>
            </Expander>
            <Expander IsExpanded="True" Header="Copy  files" Visibility="{Binding CopyFilesOperationsVisibility}">
                <GroupItem x:Name="Copy"  Margin="5">
                    <ItemsControl Name="CopyFilesOperationsGroupBox" VerticalAlignment="Top" ItemsSource="{Binding CopyFilesOperations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Content}" Command="{Binding ClickedCommand}" Margin="3" HorizontalContentAlignment="Left"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupItem>
            </Expander>
            <Expander IsExpanded="True" Header="Copy text from files" Visibility="{Binding CopyTextFromFilesOperationsVisibility}">
                <GroupItem x:Name="CopyTextFromFilesOperationsGroupBox">
                    <ItemsControl Name="CopyTextFromFilesOperations" VerticalAlignment="Top" ItemsSource="{Binding CopyTextFromFilesOperations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Content}" Command="{Binding ClickedCommand}" Margin="3" HorizontalContentAlignment="Left"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupItem>
            </Expander>
            <Expander IsExpanded="True" Header="Remove files" Visibility="{Binding RemoveFilesOperationsVisibility}">
                <GroupItem x:Name="RemoveFilesOperationsGroupBox"  Margin="5">
                    <ItemsControl Name="RemoveFilesOperations" VerticalAlignment="Top" ItemsSource="{Binding RemoveFilesOperations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Content}" Command="{Binding ClickedCommand}" Margin="3" HorizontalContentAlignment="Left"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupItem>
            </Expander>
        </StackPanel>
        <TextBox x:Name="textBox" Grid.ColumnSpan="3" HorizontalAlignment="Left" Height="23" Margin="2,-61,-403,0" Text="{Binding SourcePath}" VerticalAlignment="Top" Width="558" Grid.Column="1"/>
        <TextBox x:Name="textBox1" HorizontalAlignment="Left" Height="23" Margin="2,-33,-384,0" Text="{Binding DestinationPath}" VerticalAlignment="Top" Width="557" Grid.ColumnSpan="3" Grid.Column="1"/>
        <Label Content="Destination path:" HorizontalAlignment="Left" Margin="6,-35,0,0" VerticalAlignment="Top"/>
        <Label Content="Source path:" HorizontalAlignment="Left" Margin="6,-64,0,0" VerticalAlignment="Top"/>
        <Button Content="Browse..." Grid.Column="3" HorizontalAlignment="Left" Margin="436,-61,-492,0" VerticalAlignment="Top" Width="75" Command="{Binding SelectSourcePath}"/>
        <Button Content="Browse.." Grid.Column="3" HorizontalAlignment="Left" Margin="436,-32,-487,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</Window>
